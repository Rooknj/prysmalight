version: "3"
services:
  broker:
    build: .
    image: lightapp2-broker-prod
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - /mqtt/config
      - /mqtt/data
      - /mqtt/log


