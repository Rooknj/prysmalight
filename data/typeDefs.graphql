type Query {
  getFortuneCookie: String
  light(name: String): Light
  allLights: [Light]
  identify: Light
}

# type Mutation { # Need to fix this
#   setPower: 
#   setBrightness:
#   setSaturation:
#   setHue:
# }

type Light {
  id: Int
  name: String # name of accessory
  power: Boolean, # curent power status
  brightness: Int, # current brightness
  hue: Int, # current hue
  saturation: Int, # current saturation
}
