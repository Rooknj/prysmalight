sudo: required
services:
- docker
matrix:
  include:
  - language: node_js  
    node_js: stable
    env: BUILD_ENV=client
  - language: node_js 
    node_js: stable
    env: BUILD_ENV=api
  - language: node_js 
    node_js: stable
    env: BUILD_ENV=controller
  - language: node_js 
    node_js: stable
    env: BUILD_ENV=broker
before_install:
  - chmod +x build.sh
script: ./build.sh


