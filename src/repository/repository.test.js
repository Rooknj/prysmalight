// Creates a mock db
const createMockDependencies = () => {
  let mockDB = {};
  let mockPubsub = {};
  return { db: mockDB, pubsub: mockPubsub };
};

describe("connections", () => {
  test("Test", () => {});
});
