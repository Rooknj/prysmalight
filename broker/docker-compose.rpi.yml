version: "3"
services:
  broker:
    build:
      context: .
      dockerfile: rpi.Dockerfile
    image: lightapp2-broker-rpi
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./rpi-config:/mqtt/config
      - /mqtt/data
      - /mqtt/log


